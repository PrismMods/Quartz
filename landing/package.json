{
  "name": "quartz-landing-app",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js server and frontend for the Quartz landing page.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "node --check src/server.js && node --check src/public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
